{% extends 'base.html.twig' %}

{% block title %}Add Etudiant{% endblock %}

{% block body %}
    <h1>Add New Etudiant</h1>

    {{ form_start(form) }}
        {{ form_row(form.nom) }}  {# Render the 'nom' field #}
        {{ form_row(form.prenom) }} {# Render the 'prenom' field #}
        {{ form_row(form.email) }} {# Render the 'email' field #}
        <button type="submit" class="btn btn-primary">Add Etudiant</button>
    {{ form_end(form) }}

    <a href="{{ path('data_app') }}" class="btn btn-secondary">Back to List</a>
{% endblock %}
